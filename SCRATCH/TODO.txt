knot - the language
tangle - the node graph editor for the language
twine - a basic RTE using the language
beaver - a 3d print slicer using the language to customize slicing




# knot and tangle
next steps:
- define language spec
- map each part of language spec to node graph represenattion and ensure 1-1ness
- define commenting/annotation spec to nodes can be positioned in code

open questions:
- dealing with global inputs
- dealing with outputs
- built in moands and do operation for IO and stuf
    - but need to figure out node syntax for dealing with tihs.
- containers/interfaces
    - built in set of typeclasses but allow use defined containers to implement them?
- logging (allowable side effects, but come up some user stories so yo ucan desgin it better)


# twine
- use kumiki as a case study
- detail out laziness
- detail out caching
- detail out what triggers code execution
- figure out IO of course
- detail out reverse execution solving possibilities and limitations



-----

scratch area

top level variable declarations allowd, these just behave like file scoped let bindings I think?

A scope is a function is a box around scoped expressions in the node graph

dragging from outside into a scope is capturing, you can not drag from a scope to the outside without going through an output

there is a function list of all available functions, and it is scoped to whatever box you're in

hidden function nodes only show up in the function the list

shown functions appear in the UI where they got defined




