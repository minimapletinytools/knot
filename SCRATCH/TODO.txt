knot - the language
tangle - the node graph editor for the language
twine - a basic RTE using the language
beaver - a 3d print slicer using the language to customize slicing




# knot and tangle
next steps:
- define language spec
- map each part of language spec to node graph represenattion and ensure 1-1ness
- define commenting/annotation spec to nodes can be positioned in code

open questions:
- dealing with global inputs
- dealing with outputs
- built in moands and do operation for IO and stuf
    - but need to figure out node syntax for dealing with tihs.
- containers/interfaces
    - built in set of typeclasses but allow use defined containers to implement them?
- logging (allowable side effects, but come up some user stories so yo ucan desgin it better)


# twine
- use kumiki as a case study
- detail out laziness
- detail out caching
- detail out what triggers code execution
- figure out IO of course
- detail out reverse execution solving possibilities and limitations


------

the language supports 2 special things in addition via its annotations

1. function pullbacks (unravel); which provide nistructions on how to modify inputs to obtain given output.
all bulit in functions have default unravles, but they may be too tangled, so users are expected to provide higher level unravels on their defined functions (knots) to keep things simple, the AI is expected to assist a lot here.


2. custom output modifiers (todo cute name): we may want to unravel from a specific point in the node graph rather than its final output. in UI a specific  output for many node gets dragged into a custom output modifier node. This node may be bound to one of the final output objects using some function which has applicatino specific interpretation. For eamxlpe, we may have a series of pillars, and the num_pillars output (from a constant value node say) determines the # of pillars, we drag that out to a custom output modifier node and then we drag the output pillar object to this node as well usin ga function that displays the # pillar modifier box each time you select any of the pillars. in code, I guess this is an annotation on the output (as a let bound variable) and the annotation takes a function that can somehow read soem of teh final outputs of the graph and also use app specific functions to bind different output modifier interfaces to the outputs of the graph)






-----

scratch area

top level variable declarations allowd, these just behave like file scoped let bindings I think?

A scope is a function is a box around scoped expressions in the node graph

dragging from outside into a scope is capturing, you can not drag from a scope to the outside without going through an output

there is a function list of all available functions, and it is scoped to whatever box you're in

hidden function nodes only show up in the function the list

shown functions appear in the UI where they got defined




